#include <stdio.h>
int main()
{
    int a, r = 0, re, o;
    scanf("%d", &a);
    o = n;
       while( a!=0 )
    {
        r = a%10;
        r = *10 + r;
      a /= 10;
    }
        if (o == r)
        printf("%d is a palindrome.", o);
    else
        printf("%d is not a palindrome.", o);
       }
